<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="1800 seconds">
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <appender name="SYSLOG" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>${catcher:-localhost:8514}</destination>
        <!-- includeCallerData会产生一定的性能开销，如果压力较大的服务，可以关闭该功能 -->
        <includeCallerData>false</includeCallerData>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeCallerData>false</includeCallerData>
        </encoder>
        <!-- 超过5分钟没有新日志产生，则发送一条空日志用作保持连接 -->
        <keepAliveDuration>5 minutes</keepAliveDuration>
        <!-- 断开后尝试重连的时间间隔，避免过于频繁-->
        <reconnectionDelay>5 second</reconnectionDelay>
        <!-- 消息实际缓冲条数 -->
        <ringBufferSize>16384</ringBufferSize>
        <!-- 30分钟重新连接，配合LVS负载均衡-->
        <connectionStrategy>
            <roundRobin>
                <connectionTTL>30 minutes</connectionTTL>
            </roundRobin>
        </connectionStrategy>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{60} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="RollingFileInfo" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/info.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- rollover daily -->
            <fileNamePattern>logs/info-%d{yyyy-MM-dd}-%i.log</fileNamePattern>
            <maxHistory>10</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
            <timeBasedFileNamingAndTriggeringPolicy
                    class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <!-- or whenever the file size reaches 51200KB -->
                <maxFileSize>51200KB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{60} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="eshop-storm-logs" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/eshop-storm-logs.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- rollover daily -->
            <fileNamePattern>logs/eshop-storm-logs-%d{yyyy-MM-dd}-%i.log</fileNamePattern>
            <maxHistory>10</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
            <timeBasedFileNamingAndTriggeringPolicy
                    class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <!-- or whenever the file size reaches 51200KB -->
                <maxFileSize>51200KB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{60} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="eshop-storm" additivity="false">
        <appender-ref ref="alertSinkLogs"/>
        <appender-ref ref="SYSLOG"/>
    </logger>

    <root level="info">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="RollingFileInfo"/>
        <appender-ref ref="SYSLOG"/>
    </root>

</configuration>
